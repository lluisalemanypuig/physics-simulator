// File used for documentation purposes only

/**
 * @brief Simulator namespace.
 *
 * Main namespace of this library.
 */
namespace sim {

    /**
     * @brief Geometry objects.
     *
     * Namespace containing geometrical objects whose
     * position does not depend on the interaction with
     * other objects.
     */
    namespace geom { }

} // -- namespace sim
